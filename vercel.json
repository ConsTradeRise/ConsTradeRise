{
  "version": 2,
  "buildCommand": "npx prisma generate",
  "functions": {
    "server.js": {
      "memory": 1024,
      "maxDuration": 30,
      "includeFiles": "public/**|utils/**|middleware/**|routes/**|prisma/schema.prisma"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/server.js" }
  ]
}
